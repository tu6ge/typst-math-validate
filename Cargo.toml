[package]
name = "typst-math-validate"
version = "0.1.0"
edition = "2024"
description = "Validate Typst math markup with actionable English diagnostics"
license = "MIT OR Apache-2.0"
readme = "README.md"

[dependencies]
typst-syntax = "0.15"
